# Copy to .env.local (never commit .env.local). Server-only — not exposed to the browser.
# Do NOT prefix with NEXT_PUBLIC_ (that would bundle the key for clients).

YOUTUBE_API_KEY=

# Optional: long random string. Used to call POST /api/revalidate-youtube after a new YouTube upload.
# REVALIDATE_SECRET=
